import React from 'react';

const ReusableCard = ({ title }) => {
  return (
    <div className="card">
      <h3>{title}</h3>
      {/* Add card content */}
    </div>
  );
};

export default ReusableCard;
